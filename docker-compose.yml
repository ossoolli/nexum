services:
  governor:
    build:
      context: ./governor_agent
      dockerfile: Dockerfile.governor
    volumes:
      - .:/app
    restart: always

  pdf_processor:
    image: python:3.12-slim
    volumes:
      - .:/app
    command: tail -f /dev/null
    restart: always
